# Runtime / secrets (never commit). « data/ » couvre tout le répertoire ; lignes
# ci-dessous explicites pour data/prompts et data/output (même effet, plus lisible).
data/
data/prompts/
data/output/
.env
.env.local

# Python
.venv/
venv/
python/.venv/
.pytest_cache/
python/.pytest_cache/
*.egg-info/
__pycache__/
*.py[cod]
dist/
build/
.coverage
htmlcov/
coverage.xml

# Node / Next.js (monorepo paths)
apps/web/node_modules/
apps/web/.next/

# Logs & OS
*.log
.DS_Store
